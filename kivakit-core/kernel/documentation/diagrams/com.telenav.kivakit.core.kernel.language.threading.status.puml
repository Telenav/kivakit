@startuml

    !include ../lexakai.theme
    
    title "com.telenav.kivakit.core.kernel.language.threading.status"
    
    class ReentrancyTracker
    {
        --
        +boolean enter()
        --(checks)--
        +boolean hasReentered()
        --(lifecycle)--
        +void exit()
    }
    
    ThreadSnapshot "1" o-- "*" ThreadStatus
    class ThreadSnapshot
    {
        --(constructors)--
        +ThreadSnapshot()
        --
        +ThreadSnapshot update()
        --(collection)--
        +List<ThreadStatus> threads()
        --(time)--
        +Duration cpuTime()
        +Time time()
    }
    
    class ThreadStatus
    {
        --
        +Thread.State state()
        --(checks)--
        +boolean isDaemon()
        +boolean isKivaKit()
        --(naming)--
        +String name()
        --(time)--
        +Duration cpuTime()
    }
    
    enum WakeState
    {
        COMPLETED
        INTERRUPTED
        TIMED_OUT
    }
    
@enduml

